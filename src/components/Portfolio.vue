<template>
  <section id="portfolio" class="container">
    <h1>portfolio</h1>
    <ul class="list row">
      <li v-for="item in data" class="col-md-6">
        <img :src="item.imgUrl" alt="1" />
        <h3>{{ item.title }}</h3>
        <p>{{ item.desc }}</p>
        <small class="mb-3 text-secondary">{{ item.skills }}</small>
        <router-link class="btn btn-primary" :to="'/detail/' + item.id">상세보기</router-link>
      </li>
    </ul>
  </section>
</template>
<script setup>
const props = defineProps({
  data: Array,
}); // defineProps 함수를 사용하여 부모 컴포넌트로부터 데이터를 전달받습니다.
</script>
<style lang="scss">
#portfolio {
  h1 {
    margin-bottom: 1rem;
  }
  .list {
    list-style-type: none;
    padding: 0;

    li {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      margin-bottom: 10vh;
      img {
        width: 100%;
        aspect-ratio: 16 / 9;
        object-fit: cover;
        object-position: top;
      }
      h4 {
        margin-top: 1rem;
      }
    }
  }
}
</style>
